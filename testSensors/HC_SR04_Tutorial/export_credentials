# Description: Exports the JSON key for the sensor.
# This environment variable only lasts for a single session.
# You must repeat this step for every session.

echo "Obtaining credentials..."

export GOOGLE_APPLICATION_CREDENTIALS="iotedu_tutorial_JSON_Key.json"

echo "Credentials obtained!"
